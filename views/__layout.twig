<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.2/css/all.min.css"  rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.12.1/font/bootstrap-icons.min.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
            <a class="navbar-brand" href="#"><i class="fa-solid fa-film"></i></a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Главная</a>
                </li>
                {% for type in types %}
                    <li class="nav-item">
                        <a class="nav-link" href="/?type={{type.type_name}}">{{type.type_name|title}}</a>
                    </li>
                {% endfor %}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/search">Поиск</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/cinema-objects/create">Добавить</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/object-types/create">Тип</a>
                </li>
            </ul>
            <form action="/set-welcome/">
                <input type="text" name = "message" class = "form-control mr-2 ml-2">
            </form>
            </div>
        </div>
    </nav>
    <div class = "container">
        <div class = "row">
            <div class = "col-md-8">
                {% block content %}
                {% endblock %}
            </div>
            <div class = "col-md-4">
                <br>
                <p>Последние страницы, которые вы посетили:</p>
                {% for page in page_list %}
                    <div class = "card position-relative" style = "background-color: rgb(231, 201, 232); padding: 8px; margin: 8px">
                        <a style = "color:black" href="{{page}}">{{page}}</a>
                    </div>
                {% endfor %}
            </div>
        </div>
        {# {% block content %}
        {% endblock %} #}
    </div>
</body>
</html>